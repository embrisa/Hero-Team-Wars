# Diagnostic Logs

Runtime logs belong here during local development. Logs must use correlation IDs and redact secrets. MCP protocol messages must never be mixed into these files as undocumented free-form output.
